<UserControl x:Class="Anderson.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Anderson.Views"
             mc:Ignorable="d" d:DesignWidth="790" Height="430">
    <Grid>
        <Button Content="New User" Command="{Binding Path=FirstButton_Clicked}"  Height="47" Margin="353,303,331,80"/>
        <ListBox ItemsSource="{Binding Path=SavedUsers}" SelectedItem="{Binding Path=SelectedUser, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="100" Margin="184,119,0,0" VerticalAlignment="Top" Width="447"/>
    </Grid>
</UserControl>
